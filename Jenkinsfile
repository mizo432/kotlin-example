pipeline {
  agent any
  stages {
    stage('build') {
    // some block
      steps {
        echo 'building pipeline...'      
      }
    }
    stage('test') {
      // some block
      steps {
        echo 'testing pipeline...'      
      }
    }
    stage('deploy') {
      // some block
      steps {
        echo 'deploying pipeline...'      
      }
    }
  }
}
