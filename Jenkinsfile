pipeline {
  agent any
  stages {
    stage('common-build') {
    // some block
      steps {
        echo 'building common...'      
      }
    }
    stage('shared-build') {
    // some block
      steps {
        echo 'building shared...'      
      }
    }
    stage('build') {
    // some block
      steps {
        echo 'building pipeline...'      
      }
    }
  }
}
