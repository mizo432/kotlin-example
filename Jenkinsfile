pipline {
  agent any
  stage('build') {
    // some block
    steps {
    }
　　　　}
  stage('test') {
    // some block
    steps {
    }
　　　　}
  stage('deploy') {
    // some block
    steps {
    }
　　　　}
}
