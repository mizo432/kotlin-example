pipline {
  agent any
  stage('build') {
    // some block
    steps {
      echo 'building pipeline...'      
    }
　　　　}
  stage('test') {
    // some block
    steps {
      echo 'testing pipeline...'      
    }
　　　　}
  stage('deploy') {
    // some block
    steps {
      echo 'deploying pipeline...'      
    }
　　　　}
}
