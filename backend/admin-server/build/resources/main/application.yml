
  config:
    import: "optional:configserver:http://localhost:8888"
logging:
  file:
    name: ./build/logs/adminServerApplication.log

management:
  endpoint:
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include: "*"
  security:
    enabled: false